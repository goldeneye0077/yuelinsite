---
phase: 01-platform-foundation-theme-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - compose.yaml
  - .env.example
  - README.md
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/src/main.tsx
  - frontend/Dockerfile
  - backend/pyproject.toml
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/app/db/session.py
  - backend/alembic.ini
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/Dockerfile
autonomous: true
requirements:
  - GLOB-01
  - GLOB-03
  - GLOB-05
user_setup: []
must_haves:
  truths:
    - Developers can boot the frontend, backend, and database with one Docker Compose command.
    - The repository has a working React + Vite frontend and FastAPI backend baseline with smoke-test commands.
    - The backend exposes health endpoints and can connect to the PostgreSQL container through shared environment configuration.
  artifacts:
    - compose.yaml defines frontend, backend, and db services.
    - frontend/package.json defines dev, build, and test scripts for the shell app.
    - backend/pyproject.toml, backend/app/main.py, and backend/alembic.ini establish the Python service baseline.
  key_links:
    - compose.yaml injects the shared database contract into backend runtime and startup flow.
    - README.md documents the same commands that the Docker and package files implement.
---

<objective>
Scaffold the repository-level runtime foundation for the website.

Purpose: Establish the engineering baseline before routing, locale logic, or shell UI work begins.
Output: A runnable frontend/backend/db stack, health endpoints, migration baseline, and smoke-test tooling.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-theme-shell/01-CONTEXT.md
@.planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md
@.planning/phases/01-platform-foundation-theme-shell/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the frontend and backend application scaffolds</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/src/main.tsx, backend/pyproject.toml, backend/app/main.py, backend/app/core/config.py, README.md</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-platform-foundation-theme-shell/01-CONTEXT.md, .planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md, frontend/package.json, backend/pyproject.toml, backend/app/main.py, README.md</read_first>
  <action>Create the initial application skeleton with TypeScript Vite on the frontend and FastAPI on the backend. Use `npm` scripts on the frontend with exact commands `vite --host 0.0.0.0`, `vite build`, and `vitest run`. Use a Python 3.11 `pyproject.toml` on the backend with `fastapi`, `uvicorn[standard]`, `sqlalchemy`, `alembic`, `psycopg`, `pydantic-settings`, and `pytest`. Implement `backend/app/main.py` with an app factory or module-level `FastAPI` instance that includes `/api/health` and `/api/ready` routes. Add a root `README.md` section that documents local startup via Compose and the verification commands planned for this phase.</action>
  <verify>docker compose config && docker compose run --rm frontend npm run build && docker compose run --rm backend pytest -q</verify>
  <acceptance_criteria>
    - frontend/package.json contains `"dev": "vite --host 0.0.0.0"`, `"build": "vite build"`, and `"test": "vitest run"`
    - backend/pyproject.toml contains `fastapi`, `sqlalchemy`, `alembic`, `psycopg`, and `pytest`
    - backend/app/main.py contains `/api/health` and `/api/ready`
    - README.md contains `docker compose up --build`
  </acceptance_criteria>
  <done>Both application roots exist with runnable package definitions, backend health routes, and documented startup commands.</done>
</task>

<task type="auto">
  <name>Task 2: Add Docker Compose, Dockerfiles, and database bootstrap wiring</name>
  <files>compose.yaml, .env.example, frontend/Dockerfile, backend/Dockerfile, backend/app/core/config.py, backend/app/db/session.py, backend/alembic.ini</files>
  <read_first>.planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md, .env.example, compose.yaml, frontend/Dockerfile, backend/Dockerfile, backend/app/core/config.py, backend/app/db/session.py, backend/alembic.ini</read_first>
  <action>Create a root `compose.yaml` with services named `frontend`, `backend`, and `db`. Use PostgreSQL environment values `POSTGRES_DB=yuelin_site`, `POSTGRES_USER=yuelin`, and `POSTGRES_PASSWORD=yuelin_dev`. Add a Postgres healthcheck and make backend startup wait on healthy database state. Set `.env.example` to expose `DATABASE_URL=postgresql+psycopg://yuelin:yuelin_dev@db:5432/yuelin_site` plus any frontend API base URL needed by later plans. Add Dockerfiles for frontend and backend that support local development inside Compose. Implement backend config/session files so the database URL is loaded from settings and a SQLAlchemy session factory exists for later features. Add `backend/alembic.ini` so migrations can be introduced without restructuring the app.</action>
  <verify>docker compose config</verify>
  <acceptance_criteria>
    - compose.yaml contains `frontend:`, `backend:`, and `db:`
    - compose.yaml contains `healthcheck:` for the `db` service
    - .env.example contains `POSTGRES_DB=yuelin_site`, `POSTGRES_USER=yuelin`, `POSTGRES_PASSWORD=yuelin_dev`, and `DATABASE_URL=postgresql+psycopg://yuelin:yuelin_dev@db:5432/yuelin_site`
    - backend/app/db/session.py contains `create_engine(` or `sessionmaker(`
    - backend/alembic.ini contains `script_location`
  </acceptance_criteria>
  <done>The repo can resolve container configuration and has a shared database contract wired into backend config.</done>
</task>

<task type="auto">
  <name>Task 3: Establish smoke-test and verification infrastructure for the foundation</name>
  <files>frontend/package.json, frontend/vitest.config.ts, backend/tests/conftest.py, backend/tests/test_health.py, README.md</files>
  <read_first>.planning/phases/01-platform-foundation-theme-shell/01-VALIDATION.md, frontend/package.json, frontend/vitest.config.ts, backend/tests/conftest.py, backend/tests/test_health.py, README.md</read_first>
  <action>Add `Vitest` configuration for the frontend and a minimal `pytest` fixture/test setup for the backend. Create `backend/tests/test_health.py` to verify `/api/health` returns a 200 response and a JSON shape that can be extended later. Ensure the frontend package includes a `test` script that runs in CI-friendly non-watch mode. Update `README.md` to list the quick and full validation commands from the Phase 1 validation strategy.</action>
  <verify>docker compose run --rm frontend npm run test -- --run && docker compose run --rm backend pytest -q</verify>
  <acceptance_criteria>
    - frontend/vitest.config.ts exists and contains `defineConfig`
    - frontend/package.json contains `"test": "vitest run"`
    - backend/tests/test_health.py contains `def test_health_endpoint`
    - README.md contains `docker compose run --rm frontend npm run test -- --run`
    - README.md contains `docker compose run --rm backend pytest -q`
  </acceptance_criteria>
  <done>Foundational test commands exist and can be used as the recurring verification loop for later plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose config` succeeds
- [ ] `docker compose run --rm frontend npm run build` succeeds
- [ ] `docker compose run --rm frontend npm run test -- --run` succeeds
- [ ] `docker compose run --rm backend pytest -q` succeeds
</verification>

<success_criteria>

- All tasks completed
- Frontend, backend, and db services are defined from one root Compose file
- Backend health routes and database config baseline exist
- Smoke-test commands are documented and runnable

</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-theme-shell/01-01-SUMMARY.md`
</output>
