---
phase: 01-platform-foundation-theme-shell
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - frontend/src/styles/tokens.css
  - frontend/src/styles/global.css
  - frontend/src/features/theme/ThemeProvider.tsx
  - frontend/src/features/theme/useTheme.ts
  - frontend/src/lib/charts/theme.ts
  - frontend/src/layouts/SiteShell.tsx
  - frontend/src/components/navigation/Header.tsx
  - frontend/src/components/navigation/Footer.tsx
  - frontend/src/components/navigation/MobileNav.tsx
  - frontend/src/pages/HomeShellPage.tsx
  - frontend/src/pages/SectionPlaceholderPage.tsx
  - frontend/src/components/navigation/Header.test.tsx
  - frontend/src/features/theme/theme.test.tsx
autonomous: true
requirements:
  - GLOB-02
  - GLOB-03
  - GLOB-05
user_setup: []
must_haves:
  truths:
    - Users can navigate the core sections from a consistent shared header and footer.
    - Users can switch between light and dark themes without losing readability of the shell or chart-ready surfaces.
    - The first shell impression feels deliberate and industrial instead of template-like.
  artifacts:
    - frontend/src/styles/tokens.css and frontend/src/features/theme/ThemeProvider.tsx define the theme system.
    - frontend/src/components/navigation/Header.tsx, Footer.tsx, and MobileNav.tsx implement the global shell controls.
    - frontend/src/lib/charts/theme.ts exports theme-ready chart tokens.
  key_links:
    - ThemeProvider applies root theme attributes that token files and chart theme helpers consume.
    - SiteShell connects locale content, language switching, theme switching, and placeholder route pages.
---

<objective>
Build the visible global shell and design foundation for the bilingual site.

Purpose: Convert the foundational routes and contracts into a durable light/dark shell that later content phases can plug into.
Output: Theme tokens, theme persistence, shared header/footer/mobile nav, chart theme adapter, and placeholder route templates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-theme-shell/01-CONTEXT.md
@.planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md
@.planning/phases/01-platform-foundation-theme-shell/01-UI-SPEC.md
@.planning/phases/01-platform-foundation-theme-shell/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the theme token system and theme persistence layer</name>
  <files>frontend/src/styles/tokens.css, frontend/src/styles/global.css, frontend/src/features/theme/ThemeProvider.tsx, frontend/src/features/theme/useTheme.ts, frontend/src/lib/charts/theme.ts, frontend/src/features/theme/theme.test.tsx</files>
  <read_first>.planning/phases/01-platform-foundation-theme-shell/01-UI-SPEC.md, .planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md, frontend/src/styles/tokens.css, frontend/src/styles/global.css, frontend/src/features/theme/ThemeProvider.tsx, frontend/src/lib/charts/theme.ts</read_first>
  <action>Implement a token-driven theme system using `data-theme="light"` and `data-theme="dark"` on the root element. Define CSS variables for background, surface, border, text, interactive, and chart roles using the values from `01-UI-SPEC.md`. Persist theme choice in `localStorage` under `yuelin-theme` and default to the system preference when no value exists. Export a small VChart adapter from `frontend/src/lib/charts/theme.ts` that maps shell tokens to light and dark chart theme objects. Add tests that verify theme persistence and default resolution behavior.</action>
  <verify>docker compose run --rm frontend npm run test -- --run</verify>
  <acceptance_criteria>
    - frontend/src/styles/tokens.css contains selectors for `[data-theme="light"]` and `[data-theme="dark"]`
    - frontend/src/features/theme/ThemeProvider.tsx contains `yuelin-theme`
    - frontend/src/lib/charts/theme.ts exports both a light and dark chart theme object
    - frontend/src/features/theme/theme.test.tsx contains a test for persisted theme selection
  </acceptance_criteria>
  <done>The shell has a durable, test-backed theme system with shared app and chart tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Build the shared header, footer, language switch, and mobile navigation</name>
  <files>frontend/src/layouts/SiteShell.tsx, frontend/src/components/navigation/Header.tsx, frontend/src/components/navigation/Footer.tsx, frontend/src/components/navigation/MobileNav.tsx, frontend/src/components/navigation/Header.test.tsx</files>
  <read_first>.planning/phases/01-platform-foundation-theme-shell/01-CONTEXT.md, .planning/phases/01-platform-foundation-theme-shell/01-UI-SPEC.md, frontend/src/app/router.tsx, frontend/src/content/site/zh.ts, frontend/src/content/site/en.ts, frontend/src/layouts/SiteShell.tsx, frontend/src/components/navigation/Header.tsx, frontend/src/components/navigation/Footer.tsx, frontend/src/components/navigation/MobileNav.tsx</read_first>
  <action>Build a `SiteShell` that wraps the locale routes with a shared header and footer. The header must include brand mark placement, top-level nav items, a locale switcher that preserves the current route key, and a theme toggle. The mobile navigation should open from a controlled panel or drawer, keep the same section order as desktop, and retain access to the locale and theme controls. The footer must surface the core sections, address block, and a placeholder inquiry entry. Style these components with the lean, line-based Precision Industrial direction from the UI spec instead of heavy card containers. Add tests that verify the header renders key navigation entries and the locale switch preserves route context.</action>
  <verify>docker compose run --rm frontend npm run test -- --run</verify>
  <acceptance_criteria>
    - frontend/src/components/navigation/Header.tsx contains navigation items for `products`, `solutions`, `support`, `about`, and `contact`
    - frontend/src/components/navigation/Header.tsx contains both a language switch control and a theme toggle control
    - frontend/src/components/navigation/Footer.tsx contains the Shenzhen address text or a dedicated address binding
    - frontend/src/components/navigation/MobileNav.tsx contains the same top-level route keys as the header
    - frontend/src/components/navigation/Header.test.tsx contains a test for language-switch route preservation
  </acceptance_criteria>
  <done>The shell exposes consistent navigation and controls across desktop and mobile layouts.</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder route templates that establish the industrial visual baseline</name>
  <files>frontend/src/pages/HomeShellPage.tsx, frontend/src/pages/SectionPlaceholderPage.tsx, frontend/src/layouts/SiteShell.tsx, frontend/src/styles/global.css</files>
  <read_first>.planning/phases/01-platform-foundation-theme-shell/01-UI-SPEC.md, frontend/src/pages/HomeShellPage.tsx, frontend/src/pages/SectionPlaceholderPage.tsx, frontend/src/layouts/SiteShell.tsx, frontend/src/styles/global.css</read_first>
  <action>Create a homepage shell page and a reusable placeholder page template for the other core routes. The homepage shell should include a strong typographic lead area, a reserved visual anchor region, a compact trust strip, and CTA placement that foreshadows later brand storytelling without pretending Phase 2 content already exists. The generic placeholder template should present the section title, a short bilingual placeholder description, and a clear path back to inquiry or homepage. Keep the composition broad and editorial, with separators and generous spacing instead of stacked card grids.</action>
  <verify>docker compose run --rm frontend npm run build</verify>
  <acceptance_criteria>
    - frontend/src/pages/HomeShellPage.tsx contains a primary CTA matching `Request Consultation / 获取咨询`
    - frontend/src/pages/HomeShellPage.tsx contains a reserved hero or visual-anchor section
    - frontend/src/pages/SectionPlaceholderPage.tsx contains bindings for section title, placeholder description, and CTA
    - frontend/src/styles/global.css contains styles for shell-level section spacing and border/separator treatment
  </acceptance_criteria>
  <done>The routed shell looks intentional in both themes and provides stable placeholders for later content phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm frontend npm run build` succeeds
- [ ] `docker compose run --rm frontend npm run test -- --run` succeeds
- [ ] Header, footer, locale switch, and theme toggle render on all core route templates
- [ ] Light and dark themes keep shell typography and chart-ready surfaces readable
</verification>

<success_criteria>

- All tasks completed
- Shared shell components exist for the core route templates
- Theme switching is persistent and token-driven
- The visible shell reflects the Precision Industrial direction instead of a template default

</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-theme-shell/01-03-SUMMARY.md`
</output>
