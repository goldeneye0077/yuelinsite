---
phase: 01-platform-foundation-theme-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/app/router.tsx
  - frontend/src/app/providers.tsx
  - frontend/src/i18n/locales.ts
  - frontend/src/content/site/zh.ts
  - frontend/src/content/site/en.ts
  - frontend/src/content/site/types.ts
  - frontend/src/lib/api/site-client.ts
  - frontend/src/app/router.test.tsx
  - backend/app/api/routes/site.py
  - backend/app/api/routes/inquiries.py
  - backend/app/schemas/site.py
  - backend/app/schemas/inquiry.py
  - backend/app/api/router.py
  - backend/tests/test_site_contract.py
autonomous: true
requirements:
  - GLOB-01
  - GLOB-02
user_setup: []
must_haves:
  truths:
    - Users can access mirrored `zh` and `en` route trees with the same information structure.
    - The frontend has a typed locale content model and a typed API client boundary for shell data.
    - The backend publishes bootstrap and inquiry contract schemas ready for later implementation.
  artifacts:
    - frontend/src/app/router.tsx defines the locale-prefixed route tree.
    - frontend/src/content/site/zh.ts and frontend/src/content/site/en.ts export the same content shape.
    - backend/app/api/routes/site.py and backend/app/schemas/inquiry.py define the Phase 1 contract surface.
  key_links:
    - The locale switch logic uses a route key model that can swap only the locale segment.
    - Frontend client types align with the backend bootstrap and inquiry schemas.
---

<objective>
Implement the bilingual route and contract layer that the shared shell will sit on.

Purpose: Make language support and API boundaries concrete before the theme shell is built.
Output: Locale-prefixed routes, typed bilingual content modules, and backend site/inquiry contract endpoints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-theme-shell/01-CONTEXT.md
@.planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md
@.planning/phases/01-platform-foundation-theme-shell/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the locale-prefixed route tree and supported-locale utilities</name>
  <files>frontend/src/app/router.tsx, frontend/src/app/providers.tsx, frontend/src/i18n/locales.ts, frontend/src/app/router.test.tsx</files>
  <read_first>.planning/phases/01-platform-foundation-theme-shell/01-CONTEXT.md, .planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md, frontend/src/app/router.tsx, frontend/src/app/providers.tsx, frontend/src/i18n/locales.ts, frontend/src/app/router.test.tsx</read_first>
  <action>Create a locale model with exactly two supported values: `zh` and `en`. Build the React Router tree under `/:locale` with routes for `/`, `/products`, `/solutions`, `/support`, `/about`, and `/contact`. Add locale validation/normalization so invalid locale segments redirect to `/zh`. Add tests that verify the expected route keys exist under both locales and that the default redirect lands on `/zh`.</action>
  <verify>docker compose run --rm frontend npm run test -- --run</verify>
  <acceptance_criteria>
    - frontend/src/i18n/locales.ts contains `export const SUPPORTED_LOCALES = ["zh", "en"]`
    - frontend/src/app/router.tsx contains route paths for `products`, `solutions`, `support`, `about`, and `contact`
    - frontend/src/app/router.tsx contains a redirect or navigation fallback to `/zh`
    - frontend/src/app/router.test.tsx contains a test for invalid locale fallback
  </acceptance_criteria>
  <done>The route tree supports both locales with a stable section map and verified locale fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce typed bilingual shell content and language-switch mapping</name>
  <files>frontend/src/content/site/types.ts, frontend/src/content/site/zh.ts, frontend/src/content/site/en.ts, frontend/src/i18n/locales.ts, frontend/src/app/router.test.tsx</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-platform-foundation-theme-shell/01-CONTEXT.md, frontend/src/content/site/types.ts, frontend/src/content/site/zh.ts, frontend/src/content/site/en.ts, frontend/src/i18n/locales.ts</read_first>
  <action>Create a shared `SiteContent` type for nav labels, footer labels, route titles, company basics, and placeholder section copy. Implement `zh` and `en` modules with the same object shape and the same route keys. Add helper logic that maps the current route key to the alternate locale by preserving the path segment after the locale. Include tests or assertions that fail if the two locale objects drift in structure.</action>
  <verify>docker compose run --rm frontend npm run test -- --run</verify>
  <acceptance_criteria>
    - frontend/src/content/site/types.ts contains `export interface SiteContent`
    - frontend/src/content/site/zh.ts contains `productCenter`, `solutions`, `support`, `about`, and `contact`
    - frontend/src/content/site/en.ts contains the same keys as frontend/src/content/site/zh.ts
    - frontend/src/i18n/locales.ts or frontend/src/app/router.tsx contains helper logic that swaps only the locale segment when changing language
  </acceptance_criteria>
  <done>Locale-specific content is typed, mirrored, and ready to drive the global shell without route ambiguity.</done>
</task>

<task type="auto">
  <name>Task 3: Define the backend bootstrap and inquiry contracts with frontend client bindings</name>
  <files>backend/app/api/router.py, backend/app/api/routes/site.py, backend/app/api/routes/inquiries.py, backend/app/schemas/site.py, backend/app/schemas/inquiry.py, frontend/src/lib/api/site-client.ts, backend/tests/test_site_contract.py</files>
  <read_first>.planning/phases/01-platform-foundation-theme-shell/01-RESEARCH.md, backend/app/api/router.py, backend/app/api/routes/site.py, backend/app/api/routes/inquiries.py, backend/app/schemas/site.py, backend/app/schemas/inquiry.py, frontend/src/lib/api/site-client.ts, backend/tests/test_site_contract.py</read_first>
  <action>Create backend schemas for a shell bootstrap payload and an inquiry submission contract. The bootstrap payload should include supported locales, company name, address, primary nav items, footer groups, and theme options. The inquiry request should include `companyName`, `contactName`, `email`, `phone`, `interestCategory`, `message`, `locale`, and `sourcePage`. Implement `GET /api/v1/site/bootstrap` to return placeholder shell data for `zh` and `en`. Implement `POST /api/v1/inquiries` as a contract-first endpoint that returns `501 Not Implemented` with a clear message that persistence is planned for Phase 5. Create a frontend `site-client.ts` that types these endpoints explicitly instead of using ad hoc fetch calls. Add backend tests that verify the bootstrap response and the inquiry contract/error shape.</action>
  <verify>docker compose run --rm backend pytest -q</verify>
  <acceptance_criteria>
    - backend/app/api/routes/site.py contains `@router.get("/site/bootstrap")`
    - backend/app/api/routes/inquiries.py contains `@router.post("/inquiries")`
    - backend/app/schemas/inquiry.py contains `companyName`, `contactName`, `interestCategory`, `locale`, and `sourcePage`
    - backend/app/api/routes/inquiries.py contains `Phase 5` or `Not Implemented`
    - frontend/src/lib/api/site-client.ts contains typed functions for `getSiteBootstrap` and `submitInquiry`
  </acceptance_criteria>
  <done>The frontend and backend share a concrete bilingual shell contract and a future-ready inquiry boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose run --rm frontend npm run test -- --run` succeeds
- [ ] `docker compose run --rm backend pytest -q` succeeds
- [ ] Locale routes for `/zh` and `/en` are defined for all core sections
- [ ] Bootstrap and inquiry contracts are exposed under `/api/v1`
</verification>

<success_criteria>

- All tasks completed
- Locale-prefixed routes exist for both supported languages
- Content modules are mirrored and typed
- Backend contract routes and schemas exist for shell data and inquiries

</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-theme-shell/01-02-SUMMARY.md`
</output>
